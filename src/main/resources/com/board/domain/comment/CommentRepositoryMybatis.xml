<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.board.domain.comment.CommentRepositoryMybatis">

    <!-- 댓글 저장: 생성/수정 시각을 DB가 아닌 객체에서 설정하여 전달 -->
    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        insert into comment (post_id, parent_comment_id, author_id, author, content, created_at, updated_at)
        values (#{postId}, #{parentCommentId}, #{authorId}, #{author}, #{content}, #{createdAt}, #{updatedAt})
    </insert>

    <!-- 댓글 내용 및 수정 시각 업데이트 -->
    <update id="update">
        update comment
        set content = #{content}, updated_at = now()
        where id = #{id}
    </update>

    <!-- 작성자 이름 업데이트 (회원 정보 변경 시 연동용) -->
    <update id="updateAuthor">
        update comment
        set author = #{author}
        where id = #{id}
    </update>

    <!-- 단건 댓글 삭제 -->
    <delete id="delete">
        delete from comment
        where id = #{id}
    </delete>

    <!-- 여러 댓글 ID를 일괄 삭제 (JDBC의 루프 방식보다 효율적) -->
    <delete id="deleteAllByIds">
        delete from comment
        where id in
        <foreach item="id" collection="ids" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <!-- 전체 댓글 조회 -->
    <select id="findAll" resultType="Comment">
        select id, post_id, parent_comment_id, author_id, author, content, created_at, updated_at
        from comment
    </select>

    <!-- ID를 통한 단건 조회 -->
    <select id="findById" resultType="Comment">
        select id, post_id, parent_comment_id, author_id, author, content, created_at, updated_at
        from comment
        where id = #{id}
    </select>

    <!-- 게시물 ID별 댓글 목록 조회 (작성 순서 정렬) -->
    <select id="findAllByPostId" resultType="Comment">
        select
            c.id, c.post_id, c.parent_comment_id, m.name as author, c.author_id, c.content, c.created_at, c.updated_at
        from comment c
        join member m on c.author_id = m.id
        where c.post_id = #{postId}
        order by c.id
    </select>

    <!-- 게시물 삭제 시 관련 댓글 일괄 삭제 -->
    <delete id="deleteByPostId">
        delete from comment
        where post_id = #{postId}
    </delete>

    <!-- 계층형 구조를 위한 하위 자손 댓글 ID 재귀 조회 (WITH RECURSIVE 활용) -->
    <select id="findAllDescendantCommentIds" resultType="long">
        WITH RECURSIVE cte AS (
            SELECT id FROM comment WHERE parent_comment_id = #{parentCommentId}
            UNION ALL
            SELECT c.id FROM comment c INNER JOIN cte ON c.parent_comment_id = cte.id
        )
        SELECT id FROM cte
    </select>

</mapper>
