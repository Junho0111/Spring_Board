<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.board.domain.member.MemberRepositoryMybatis">

    <!-- 신규 회원 저장 -->
    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        insert into member (login_id, name, password)
        values (#{loginId}, #{name}, #{password})
    </insert>

    <!-- 고유 ID를 통한 회원 단건 조회 -->
    <select id="findById" resultType="Member">
        select id, login_id, name, password
        from member
        where id = #{id}
    </select>

    <!-- 로그인 ID를 통한 회원 단건 조회 -->
    <select id="findByLoginId" resultType="Member">
        select id, login_id, name, password
        from member
        where login_id = #{loginId}
    </select>

    <!-- 전체 회원 목록 조회 -->
    <select id="findAll" resultType="Member">
        select id, login_id, name, password
        from member
    </select>

    <!-- 회원 정보 업데이트 -->
    <update id="update">
        update member
        set name = #{name}, password = #{password}
        where id = #{id}
    </update>

    <!-- 회원 삭제 -->
    <delete id="delete">
        delete from member
        where id = #{id}
    </delete>

</mapper>
