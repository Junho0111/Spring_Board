<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.board.domain.uploadfile.UploadFileRepositoryMybatis">

    <!-- 파일 정보 저장 -->
    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        insert into upload_file (post_id, store_file_name, upload_file_name, file_type)
        values (#{postId}, #{storeFileName}, #{uploadFileName}, #{fileType})
    </insert>

    <!-- 게시물 ID별 파일 삭제 -->
    <delete id="deleteByPostId">
        delete from upload_file
        where post_id = #{postId}
    </delete>

    <!-- 게시물 ID별 모든 파일 조회 -->
    <select id="findAllByPostId" resultType="UploadFile">
        select id, post_id, store_file_name, upload_file_name, file_type
        from upload_file
        where post_id = #{postId}
    </select>

</mapper>
